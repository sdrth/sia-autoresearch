---
description: sia-autoresearch — founder research system
globs: ["start/**", "config/**", "runs/**", "notes/**", "dashboard/**", "examples/**", "autoresearch/**"]
alwaysApply: false
---

# sia-autoresearch

This repo uses the sia-autoresearch workflow for founders and builders.

When asked to run research, analyze the product, generate bets, explore strategy, do competitive intel, or write founder content:

1. **Detect mode and resolve paths** — see spec section 2
   - **Standalone lab (this repo):** spec = `start/AUTORESEARCH-START.md`, lab root = repo root, runs at `runs/`
   - **Embedded mode:** spec = `autoresearch/AUTORESEARCH-START.md`, lab root = `autoresearch/`
2. **Read and follow** the spec file for the detected mode
3. **Create and edit only under `<lab>/`** — in this repo, `start/` and `examples/` are read-only reference material
4. **Do not skip the startup interview** — if `<lab>/config/setup.md` is missing or not `status: complete`, run section 3 first
5. **Search before synthesis** — generate multiple hypotheses, test multiple query branches, and do not jump to the first plausible answer
6. **Preserve proof of work** — keep exact queries run, linked sources, discarded branches, and why the kept thesis won
7. **Viewer & language (Q10b–c, Q11)** — static `dashboard/index.html` unless `skip`; ask simple vs interactive; `audience.md` for plain non-technical copy
8. **Go deep every run** — web search across branches, in-repo read-only scan when context exists, subagents when available; parent keeps final score
9. **Security** — user's agent and tools only; default web search for external signals; no telemetry or sia-autoresearch API calls (spec section 2A)
10. **After every run:** post only the compact per-run index in chat (below); full artifacts in `runs/` + viewer — never paste memo/hypotheses/verdict bodies

## Quick start prompt

```
Read and follow: start/AUTORESEARCH-START.md
```

## Per-run chat index (required; not full artifacts)

After every run, post only this block — details live in the run folder and viewer:

```
## Run complete: [run-id]
**Kind:** [kind] | **Score:** [overall] | **Verdict:** [verdict]
**Topic:** [topic]
**Proof:** [N] generated, [M] researched, [K] discarded — see `hypotheses.md`
**Top findings:**
1. [one line]
2. [one line]
3. [one line]
**Run folder:** `<lab>/runs/[run-id]/`
**Viewer:** `<lab>/dashboard/index.html` (or "runs/ only" if Q10b skip)
```

Spec + source: https://github.com/sdrth/sia-autoresearch

Inspired by [Karpathy's autoresearch](https://github.com/karpathy/autoresearch).
